package me.phoenixra.visor.api.client.gui.widgets;

public enum FilterListType {
    /**
     * All active filters has to match to pass filtering
     */
    ALL,
    /**
     * At least one filter has to match to pass filtering
     */
    AT_LEAST_ONE
}
