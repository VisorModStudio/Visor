architectury {

    common(rootProject.enabled_platforms.split(","))
}
loom{
    accessWidenerPath = file("src/main/resources/visor.accesswidener")
}
dependencies {
    // @Environment annotation and mixin extras
    compileOnly(libs.fabric.loader)

    // ---- LOCAL DEPENDENCY
    //modApi name: 'atumvr-api-1.10.1beta'
    //modApi name: 'atumvr-core-1.10.1beta'
    // ----
    api(libs.atumvr.api)
    api(libs.atumvr.core)
    api(libs.atumvr.config)

    compileOnly(libs.bundles.annotations)
    annotationProcessor(libs.bundles.annotations)
}